{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trummler12.github.io/custom-wordlists/schema/category.schema.json",
  "title": "Category",
  "description": "Optional display metadata for a category folder, in a `_category.json` sidecar. Overrides the folder-name-derived title (which can't punctuate or translate) and can add a per-language name and an icon. See docs/archive/PLANNING.md §4.",
  "type": "object",
  "additionalProperties": false,
  "minProperties": 1,
  "properties": {
    "title": {
      "allOf": [{ "$ref": "#/$defs/word" }],
      "description": "Display name, in the same shape as a topic's title or a word entry: a plain string where the name is global (\"Film & TV\"), a language map where it translates, or a short/long pair — `short` on the row, `long` in a hover. Falls back to the title-cased folder name."
    },
    "icon": { "type": "string", "minLength": 1 },
    "hideRulersByDefault": {
      "type": "boolean",
      "description": "When true, the fame rulers of this category's topics start hidden behind a single toggle on the category row rather than shown. Declaring the field (either value) makes this category an independent ruler-visibility boundary: its toggle governs its subtree down to the next node that declares the field, and children without the field inherit this value."
    }
  },
  "$defs": {
    "langMapString": {
      "type": "object",
      "minProperties": 1,
      "required": ["en"],
      "description": "A per-language string: keys are language codes (\"en\" is the required base), values the localized text. Languages that match \"en\" are omitted.",
      "propertyNames": { "pattern": "^[a-z]{2}(-[A-Z]{2}|-[A-Z][a-z]{3})?$" },
      "additionalProperties": { "type": "string", "minLength": 1 }
    },
    "locString": {
      "oneOf": [{ "type": "string", "minLength": 1 }, { "$ref": "#/$defs/langMapString" }],
      "description": "A string that is the same everywhere (plain string) or translates (language map)."
    },
    "namePair": {
      "type": "object",
      "additionalProperties": false,
      "required": ["short", "long"],
      "description": "A short (row) and long (hover) form of the name; each may itself be a language map when only that part translates.",
      "properties": {
        "short": { "$ref": "#/$defs/locString" },
        "long": { "$ref": "#/$defs/locString" }
      }
    },
    "word": {
      "description": "Mirrors `word` in topic.schema.json, so a category's title has the same shape as a topic's. Repeated rather than referenced across files: the two schemas are loaded independently.",
      "oneOf": [
        { "type": "string", "minLength": 1 },
        { "$ref": "#/$defs/namePair" },
        { "$ref": "#/$defs/langMapString" }
      ]
    }
  }
}
