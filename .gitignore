# Local-only scratch: PR drafts, notes, anything not meant for the repo
_untracked/

# Node / build (for later milestones)
node_modules/
dist/
.cache/

# Generated data manifest (produced by `npm run build:index`)
data/index.json
